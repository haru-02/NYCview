services:
  image-agent:
    build: .
    container_name: nyc_poi_agent
    volumes:
      - ./output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
      - UNSPLASH_ACCESS_KEY=key
      - PEXELS_API_KEY=key
      - EMAIL=mail@gmail.com
